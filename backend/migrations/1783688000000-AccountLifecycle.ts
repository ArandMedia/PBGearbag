import { MigrationInterface, QueryRunner } from 'typeorm';
export class AccountLifecycle1783688000000 implements MigrationInterface { name='AccountLifecycle1783688000000'; async up(q:QueryRunner){await q.query(`CREATE TYPE "auth_tokens_type_enum" AS ENUM ('verify_email','password_reset')`);await q.query(`ALTER TABLE "users" ADD "email_verified_at" timestamptz, ADD "terms_accepted_at" timestamptz, ADD "age_confirmed" boolean NOT NULL DEFAULT false`);await q.query(`CREATE TABLE "auth_tokens" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(),"user_id" uuid NOT NULL,"token_hash" varchar NOT NULL UNIQUE,"type" "auth_tokens_type_enum" NOT NULL,"expires_at" timestamptz NOT NULL,"used_at" timestamptz,"created_at" timestamptz NOT NULL DEFAULT now(),CONSTRAINT "FK_auth_tokens_user" FOREIGN KEY ("user_id") REFERENCES "users"("id") ON DELETE CASCADE)`);await q.query(`CREATE INDEX "IDX_auth_tokens_user" ON "auth_tokens" ("user_id")`)} async down(q:QueryRunner){await q.query(`DROP TABLE "auth_tokens"`);await q.query(`ALTER TABLE "users" DROP COLUMN "age_confirmed", DROP COLUMN "terms_accepted_at", DROP COLUMN "email_verified_at"`);await q.query(`DROP TYPE "auth_tokens_type_enum"`)}}
