import { CanActivate,ExecutionContext,ForbiddenException,Injectable } from '@nestjs/common';import{Reflector}from'@nestjs/core';import{User}from'../../users/entities/user.entity';import{VERIFIED_KEY}from'../decorators/verified.decorator';
@Injectable()export class VerifiedGuard implements CanActivate{constructor(private r:Reflector){}canActivate(c:ExecutionContext){if(!this.r.getAllAndOverride<boolean>(VERIFIED_KEY,[c.getHandler(),c.getClass()]))return true;const u=c.switchToHttp().getRequest<{user?:User}>().user;if(!u?.isVerified)throw new ForbiddenException('Verify your email before using this feature');return true}}
